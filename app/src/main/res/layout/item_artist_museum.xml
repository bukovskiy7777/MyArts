<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <de.hdodenhof.circleimageview.CircleImageView
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:id="@+id/maker_image"
        android:layout_width="100dp"
        android:layout_height="100dp" />

    <TextView
        android:text="Rembrandt"
        android:layout_marginTop="6dp"
        android:textColor="@color/colorBlack"
        android:fontFamily="@string/font_serif_monospace"
        android:textSize="16sp"
        android:minLines="1"
        android:maxLines="2"
        android:ellipsize="end"
        android:id="@+id/maker_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        app:layout_constraintLeft_toLeftOf="@id/maker_image"
        app:layout_constraintRight_toRightOf="@id/maker_image"
        app:layout_constraintTop_toBottomOf="@id/maker_image" />

</androidx.constraintlayout.widget.ConstraintLayout>